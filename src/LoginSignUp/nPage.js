import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'
import Nav from '../../Navigation';
export default class nPage extends Component {
    render() {
        return (
            <Nav />
        )
    }
}

const styles = StyleSheet.create({})
